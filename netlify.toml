[build]
  publish = "ivxholding-landing"
  command = "echo 'No build needed'"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--prefix=/dev/null"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
